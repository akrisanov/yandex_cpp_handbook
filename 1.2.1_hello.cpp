#include <iostream>

int main()
{
    // cout – character output stream
    // depends on the OS, the \n character will be converted to one or two bytes with codes 0A or 0D 0A
    std::cout << "Hello, World!\n";
}
